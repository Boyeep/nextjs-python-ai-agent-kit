services:
  frontend:
    build:
      context: ./frontend
    ports: ["3000:3000"]
    depends_on: [backend]
  backend:
    build:
      context: ./backend
    environment:
      CORS_ALLOWED_ORIGINS: http://127.0.0.1:3000,http://localhost:3000
    ports: ["8000:8000"]
